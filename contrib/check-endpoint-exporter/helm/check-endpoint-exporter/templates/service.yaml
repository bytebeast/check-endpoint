apiVersion: v1
kind: Service
metadata:
  name: {{ include "check-endpoint-exporter.fullname" . }}
  labels:
    {{- include "check-endpoint-exporter.labels" . | nindent 4 }}
  {{- if or .Values.service.annotations .Values.service.prometheusAnnotations }}
  annotations:
    {{- with .Values.service.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    {{- if .Values.service.prometheusAnnotations }}
    prometheus.io/scrape: "true"
    prometheus.io/port: {{ .Values.exporter.port | quote }}
    prometheus.io/path: {{ .Values.exporter.path | quote }}
    {{- end }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "check-endpoint-exporter.selectorLabels" . | nindent 4 }}
  ports:
    - name: metrics
      port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
